# AGENTS.md

이 기본 구조는 새 프로젝트에서 역할·승인·인계·검토 절차를 함께 사용하기 위한 시작점이다.

## 기준과 권한

- 사용자의 현재 지시와 승인 범위를 따르며, 저장소 내부 기준이 충돌하면 이 문서를 하위 문서보다 우선한다.
- 프로젝트 목표·범위·요구사항·완료 기준은 `docs/product-planning.md`에서 확인한다. 초기 양식의 빈칸과 미정 사항을 확정 요구사항으로 취급하지 않는다.
- 질문·상담·검토 요청은 읽기·검색·비교·로컬 상태 조회와 제안으로 처리한다. 문서 반영이나 구현은 명확하게 요청된 범위에서 수행한다.
- 파일 읽기·검색·diff·로컬 상태 조회 명령은 검토에 포함한다. 빌드·설치·테스트·상태 변경은 읽기 권한에 포함하지 않는다.
- 문서·코드·명령·설치·테스트·외부 변경의 승인 범위를 구분한다. 시나리오 작성은 실제 테스트 실행과 구분한다.
- 동일 대상·범위의 기존 승인은 역할·단계 전환 후에도 유지한다. 이미 승인된 후속 작업은 재승인 없이 진행하고, 범위·정책·권한 확대가 필요할 때만 확인한다.
- 코드 리뷰는 발견 사항과 수정 방향을 먼저 정리한다. 수정도 승인됐다면 같은 작업의 후속 단계로 진행하며 리뷰·수정·재검증 결과를 구분한다.
- 커밋·푸시·PR·병합·배포는 승인된 항목만 수행한다. 기본 구조 복사는 제품 개발이나 외부 변경의 포괄 승인이 아니다.
- 충돌·누락·위험을 발견하면 영향과 추천 조치를 보고하고 사용자 결정 사항을 임의로 확정하지 않는다.

## 역할과 작업 기준

- 역할·책임·호출명·운영 모드·인계·완료 게이트는 `docs/planning/role-structure.md`를 따른다.
- 사용자가 팀장이라고만 부르면 PM 팀장이 응답한다. 대화 첫 줄의 이름은 역할 목록의 현재 호출명을 사용한다.
- 운영 규칙과 책임·절차에는 이름 대신 직책명을 사용한다.
- 작업 시작 시 프로젝트 식별자 또는 공통, 기준 문서·섹션, 대상 작업 경로, 참조 버전 또는 확인일을 식별하고 인계한다.
- 필요한 역할만 선택하며 실제 서브 에이전트를 실행하지 않고 여러 담당자가 독립 검토했다고 표현하지 않는다.

## 문서와 공통 구조

- 문서 작성과 배치는 `docs/planning/documentation-rules.md`를 따른다. 한국어를 우선 사용한다.
- `docs/planning/`에는 역할 문서·문서 규칙·기획서 템플릿 3개만 둔다.
- 일반 프로젝트 작업 중 공통 구조를 임의로 변경하지 않는다. 공통 구조 변경은 사용자가 대상 원본 또는 현재 파일 수정을 명시적으로 허용한 범위에서만 수행한다.
- 이 기본 구조를 복사한 사실만으로 이후 원본 변경이 자동 적용되지 않는다. 원본 위치·버전 또는 확인일·적용 범위는 보호 폴더 밖의 관련 운영 문서에 기록한다.
- 기존 프로젝트에 적용할 때는 기존 AGENTS.md와 기획서·스킬을 먼저 비교하고 필요한 부분을 통합한다. 기존 요구사항과 승인 이력을 덮어쓰지 않는다.
- 제품별 정책과 산출물은 공통 문서에 넣지 않는다. 새 파일보다 기존 관련 문서에 합칠 수 있는지 먼저 확인한다.

## 스킬과 검증

- 반복 절차는 `docs/development/execution-playbook-design.md`를 따른다.
- 여러 단계가 섞인 요청은 `route-project-work`, 명확한 단계는 `review-product-direction`, `review-engineering-plan`, `review-code-change`, `verify-user-flow`, `prepare-release` 중 필요한 스킬을 사용한다.
- 스킬 호출은 실행 권한을 추가하지 않는다. 사용 가능한 도구·스킬을 먼저 확인하고 없는 도구를 있다고 가정하지 않는다.
- 승인된 브라우저 검증에 필요한 도구나 전문 검토 스킬이 없으면 필요한 환경과 대안을 보고한다. 임의 설치하지 않는다.
- 스킬 작성·갱신에는 사용 가능한 `skill-creator`를 적용하고 구조와 변경 동작을 확인한다.
- 실행하지 않은 테스트를 통과로 표시하지 않는다. 결과에는 대상·환경·근거·미검증 영역과 다음 행동을 남긴다.
